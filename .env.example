# Copy this file to ".env" and paste your real OpenAI key.
#   cp .env.example .env
# Then edit .env and put your key between the quotes.
# The real .env is gitignored, so your key is never committed or shared.
OPENAI_API_KEY=""
